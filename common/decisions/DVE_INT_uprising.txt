# ============================================================
#   DvE - INT's workers' uprising
#   Narrower cousin of the Soviet International: only the 5
#   nations tied to the European civil war (CMU, SPA, GDR, PRS,
#   ENG) can receive this
# ============================================================

INT_uprising_category = {

	INT_uprising_seize_power = {
		icon = GFX_decision_revolt
		allowed = {
			OR = {
				tag = CMU
				tag = SPA
				tag = GDR
				tag = PRS
				tag = ENG
			}
		}
		visible = {
			country_exists = INT
			NOT = { has_government = vanguard_socialism }
		}
		available = {
			NOT = { is_subject_of = INT }
		}
		cost = 0
		fire_only_once = no
		complete_effect = {
			INT_uprising_seize_power_effect = yes
		}
		ai_will_do = { factor = 50 }
	}
}

INT_uprising_annex_category = {

	INT_uprising_annex_puppet = {
		icon = GFX_decision_generic_form_nation
		allowed = { tag = INT }
		targets = { CMU SPA GDR PRS ENG }
		target_trigger = {
			FROM = {
				exists = yes
				is_subject_of = ROOT
			}
		}
		available = {
			has_political_power > 149
			FROM = { NOT = { is_at_war = yes } }
		}
		cost = 150
		fire_only_once = no
		complete_effect = {
			annex_country = { target = FROM transfer_troops = yes }
		}
		ai_will_do = { factor = 20 }
	}
}
