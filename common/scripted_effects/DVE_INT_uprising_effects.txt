# INT-backed workers' uprising. Runs in the TARGET's own scope
# (ROOT = the country seizing power for itself).
INT_uprising_seize_power_effect = {

	if = {
		limit = { is_in_faction = yes }
		leave_faction = yes
	}

	if = {
		limit = { NOT = { is_subject_of = INT } }
		INT = { puppet = ROOT }
	}

	# Set AFTER puppet, so whatever the new subject relationship does
	# to the government can't leave it on INT's own ideology.
	# Keeps the nation's own vanguard_socialism party name and leader
	# (must already be defined in its history file).
	set_politics = {
		ruling_party = vanguard_socialism
		elections_allowed = no
	}
	add_popularity = { ideology = vanguard_socialism popularity = 0.60 }
	add_stability = -0.10
}
