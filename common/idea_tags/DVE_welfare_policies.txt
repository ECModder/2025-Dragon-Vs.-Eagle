# The welfare_policies category moved into common/idea_tags/
# 00_idea.txt, positioned between state_policies and
# research_production - category order follows definition order
# within a single file, so a separate file could never land there.
# Left empty rather than deleted so nothing dangles.
