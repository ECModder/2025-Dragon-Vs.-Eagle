add_namespace = finland_start

country_event = {
	id = finland_start.1
	fire_only_once = yes
	is_triggered_only = yes
	trigger = {
		tag = FIN
	}

	immediate = {
		add_stability = 0.15
		add_war_support = 0.05
		add_named_temp_variable = { name = start_bonus value = 0.05 }
		add_political_power = 50
	}
}
